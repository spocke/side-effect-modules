{
  "name": "test-pure-modules",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/app.js --bundle --outfile=lib/out.js | cat lib/out.js"
  },
  "sideEffects": ["./src/effect.js"],
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.11.20"
  }
}
